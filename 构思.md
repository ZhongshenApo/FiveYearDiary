系统设定：
你是一个资深的系统架构师和全栈开发者。我们即将开发一个基于本地 Markdown 文件的“极简五年日记” Web App。

当前目标：
这是项目的阶段一。请不要急于写完整的 UI 代码。我要你先帮我把项目的骨架搭建好，并明确各个模块的职责，防止未来迭代产生代码冗余（屎山）。

技术栈限定：

后端：Node.js + Express (只负责提供极简的 API 给前端读写本地文件，越轻量越好)。
前端：原生 HTML/JS 即可，或者极简的 React/Vite（不使用复杂的全局状态库）。
样式：Tailwind CSS（通过 CDN 引入或基础配置即可）。
核心数据规范（绝对真理）：
所有的日记数据存储在 ./data/ 目录下，按天命名，如 10-24.md。
文件内容必须严格遵循以下格式：

# 10-24

## 2024
今天发生了...

## 2023
去年今天...

目录结构设计： 给我一个清晰的项目文件树。必须严格分离“前端 UI”、“后端 API”和“Markdown 解析工具函数”。
核心解析器代码 (parser.js)： 写一个独立的函数，专门负责把上述的 Markdown 字符串解析成 JSON 对象，再写一个函数把 JSON 对象转回上述 Markdown 格式。这段逻辑必须能独立测试，不能和任何文件读写或 UI 代码混在一起。
API 设计思路： 告诉我后端准备提供哪几个接口供前端调用。
请记住，你的回答质量决定了我们这个项目未来的可维护性。先不要写花哨的界面，专注在底层的模块划分和数据解析上。