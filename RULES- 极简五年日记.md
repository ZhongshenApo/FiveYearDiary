# RULES.md - 极简五年日记 (Forward-Looking Markdown Edition) 

## 1. 项目愿景 (Project Vision)
本项目旨在开发一款**本地优先、极简主义**的“五年日记” Web 应用程序。
核心体验是模拟一本**全新的五年实体日记本**。每一页代表一年中的某一天，页面上自上而下固定印有未来的 5 个连续年份（例如 2026-2030）。用户随时间流逝，逐年填满这页纸，感受时间的积累。

## 2. 核心开发原则 (Core Principles)
* **数据主权绝对安全：** 所有日记数据**必须且只能**以纯文本 Markdown (`.md`) 格式保存在本地指定文件夹中。
* **极简至上 (Minimalism)：** 抵制一切花哨功能。只要原生 textarea，保持大量留白和纸质质感。
* **高内聚低耦合 (防腐架构)：** 严禁在前端 UI 代码中直接进行文件读写操作。严格按照分层架构开发，拒绝面条代码。

## 3. 技术栈限定 (Tech Stack)
* **后端 (数据服务)：** Node.js + Express (极轻量级，仅提供本地 API 接口供前端读写本地文件)。
* **前端 (用户界面)：** React (基于 Vite 构建) 或 极简原生 HTML/JS。
* **样式：** Tailwind CSS。
* **数据存储：** 本地文件系统 (Node.js `fs` 模块)。

## 4. 核心时间逻辑与展示规范 (Time & Display Rules - 极其重要)
这是一个“固定窗口期”的日记本，请严格遵循以下时间逻辑：
* **全局起点设定：** 项目必须存在一个全局配置（如 `.env` 或 `config.js` 中的 `START_YEAR = 2026`）。这代表这本日记的起始年份。
* **固定的 5 年卡片：** 无论用户在哪一年打开应用，UI 界面**始终且只渲染**从 `START_YEAR` 开始的连续 5 个年份卡片。以 `START_YEAR=2026` 为例，无论今天是何年，界面上永远是从上到下排列：2026、2027、2028、2029、2030。
* **状态互斥逻辑（交互灵魂）：**
  系统需要获取当前的**真实自然年份 (Current System Year)**，并以此判断卡片状态：
  1. **过去年份 (< Current Year):** 只读状态 (Read-only)，展示已记录的文字。
  2. **当前年份 (== Current Year):** 唯一**可编辑**状态 (Editable textarea)。
  3. **未来年份 (> Current Year):** 锁定状态 (Locked/Placeholder)，界面上优雅地提示“未来留白”或不可点击。

## 5. 核心数据规范 (Data Specification)
* **存储目录：** 项目根目录下的 `/data/` 文件夹。
* **文件命名规范：** `MM-DD.md` (例如：`05-01.md`)。每年共用 366 个文件。
* **Markdown 文件内部结构规范示例 (以 `START_YEAR=2026` 为例)：**
  必须是一个一级标题作为日期，后续使用二级标题标记固定的 5 个年份（按时间顺序从上到下排列）。
  即使某一年还没到来，或没有记录，`## 年份` 的标题也必须预先生成好（占位）。
  ```markdown
  # 05-01

  ## 2026
  今天是这本五年日记的第一天。天气晴。

  ## 2027
  
  ## 2028
  
  ## 2029
  
  ## 2030
  
  ```

## 6. 项目架构与职责划分 (Architecture)
项目必须严格按照以下三层结构分离：

1. **核心逻辑层 (`/server/parser.js`)：**
   - 职责：读取配置中的 `START_YEAR`。负责将 Markdown 文本解析为包含 5 个固定年份的 JSON 对象；以及将 JSON 对象组装回如上所示的、包含 5 个年份标题的 Markdown 文本。
2. **数据访问层 (`/server/api.js`)：**
   - 职责：使用 Node.js `fs` 读取/写入对应的 `.md` 文件。如果请求的 `.md` 文件不存在，利用 `START_YEAR` **初始化并返回**一个包含 5 个空年份标题的基础 Markdown 文件。
3. **用户展示层 (`/client/` 或 `/src/`)：**
   - 职责：根据后端返回的 JSON 数据，和当前系统真实年份，决定哪个卡片可编辑，哪个是只读，哪个是未来留白。不处理 Markdown 格式。

## 7. 给 AI 的强制指令 (AI Directives)
1. 在写代码前，先文字确认你对“START_YEAR 固定窗口”和“过去只读/现在可写/未来锁定”这一交互逻辑的理解。
2. 拒绝面条代码，保持函数单一职责。
3. 每次只解决一个具体的小问题，不要一次性输出所有代码。